import { OderByPipePipe } from './oder-by-pipe.pipe';

describe('OderByPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OderByPipePipe();
    expect(pipe).toBeTruthy();
  });
});
